services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: kubeforge
      POSTGRES_PASSWORD: kubeforge
      POSTGRES_DB: kubeforge
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
